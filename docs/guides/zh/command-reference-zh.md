# 命令参考 (Command Reference)

> 📖 **繁體中文指南:** [命令參考 (繁體中文)](../command-reference_zh-TW.md) | 📖 **日本語ガイドはこちら:** [コマンドリファレンス (日本語)](../ja/command-reference.md)

cc-sdd 所有命令的完整参考，包含详细用法、示例和故障排除。

> **注意**：本参考基于 Claude Code 命令模板。虽然核心功能在所有受支持的代理（Cursor, Gemini CLI, Codex CLI, GitHub Copilot, Qwen Code, Windsurf）中保持一致，但命令语法和功能可能会根据您的代理略有不同。请参阅您代理的特定文档以了解平台特定细节。

> 有关安装、CLI 设置和工作区先决条件，请参阅 [项目 README](../../README.md)。有关其他文档和指南的概述，请从 [文档 README](../README.md) 开始。

## 快速索引

### 项目引导命令 (Steering Commands)
- [`/kiro:steering`](#kirosteering) - 创建/更新项目记忆
- [`/kiro:steering-custom`](#kirosteering-custom) - 添加特定领域的引导

### 规格工作流命令 (Spec Workflow Commands)
- [`/kiro:spec-init`](#kirospec-init) - 初始化新功能规格
- [`/kiro:spec-requirements`](#kirospec-requirements) - 生成需求
- [`/kiro:spec-design`](#kirospec-design) - 创建技术设计
- [`/kiro:spec-tasks`](#kirospec-tasks) - 拆解为任务
- [`/kiro:spec-impl`](#kirospec-impl) - 执行实现

### 验证命令 (Validation Commands)
- [`/kiro:validate-gap`](#kirovalidate-gap) - 分析现有代码与需求的差距
- [`/kiro:validate-design`](#kirovalidate-design) - 评审设计质量
- [`/kiro:validate-impl`](#kirovalidate-impl) - 验证实现质量

### 状态与工具 (Status & Utility)
- [`/kiro:spec-status`](#kirospec-status) - 检查功能进度

---

### 命令矩阵

| 命令 | 参数 | 主要目的 | 典型下一步 |
|---------|------------|-----------------|-------------------|
| `/kiro:steering` | – | 引导或同步项目记忆 | `/kiro:spec-init` |
| `/kiro:steering-custom` | – (交互式) | 捕获特定领域的模式 | `/kiro:spec-init` 或重新运行 steering |
| `/kiro:spec-init` | `<项目描述>` | 创建新规格文件夹和元数据 | `/kiro:spec-requirements <feature>` |
| `/kiro:spec-requirements` | `<功能名称>` | 生成 EARS 格式需求 | `/kiro:spec-design <feature>` |
| `/kiro:validate-gap` | `<功能名称>` | (可选) 分析现有代码差距 | `/kiro:spec-design <feature>` |
| `/kiro:spec-design` | `<功能名称> [-y]` | 产生 `research.md` (需要时) + 技术设计 | `/kiro:spec-tasks <feature>` |
| `/kiro:validate-design` | `<功能名称>` | (可选) 评审设计质量 | `/kiro:spec-tasks <feature>` |
| `/kiro:spec-tasks` | `<功能名称> [-y]` | 将设计分解为带有并行安全块 (P#) 的实现任务 | `/kiro:spec-impl <feature> [tasks]` |
| `/kiro:spec-impl` | `<功能名称> [任务编号]` | 通过 TDD 执行任务 | `/kiro:validate-impl [feature] [tasks]` |
| `/kiro:validate-impl` | `[功能名称] [任务编号]` | 验证实现质量 | `/kiro:spec-status <feature>` |
| `/kiro:spec-status` | `<功能名称>` | 总结工作流进度 | 使用建议的命令继续 |

---

## 项目引导命令 (Steering Commands)

### `/kiro:steering`

**目的**：创建或更新项目记忆 (steering)，以便每个命令都能引用共享规则、架构护栏和全产品指南。它*不*用于存储特定功能的实现说明。

**参数**：无

**用法**：
```bash
/kiro:steering
```

**功能说明**：
分析您的代码库并生成/更新三个核心引导文档，这些文档捕获了长期的指导方针（而非单个功能的细节）：
- `{{KIRO_DIR}}/steering/structure.md` - 架构模式、目录组织、命名约定
- `{{KIRO_DIR}}/steering/tech.md` - 技术栈、框架决策、技术限制
- `{{KIRO_DIR}}/steering/product.md` - 业务上下文、产品目的、核心能力

---

### `/kiro:steering-custom`

**目的**：为特定领域的模式（API 标准、测试计划、UI/UX 指南等）创建专门的引导文档。

**参数**：无 (交互式)

**用法**：
```bash
/kiro:steering-custom
```

**功能说明**：
交互式命令，帮助您在 `{{KIRO_DIR}}/steering/` 中为三个核心文件之外的专门领域创建自定义引导文件。

---

## 规格工作流命令 (Spec Workflow Commands)

### `/kiro:spec-init`

**目的**：使用目录结构和元数据初始化新的功能规格。

**参数**：`<项目描述>`

**用法**：
```bash
/kiro:spec-init <项目描述>
```

**参数说明**：
- `<项目描述>` (必填)：要构建的功能的详细描述。

**功能说明**：
1. 根据您的描述生成唯一的功能名称
2. 创建规格目录：`{{KIRO_DIR}}/specs/<feature-name>/`
3. 初始化元数据文件：`spec.json`
4. 创建初始需求存根：`requirements.md`

---

### `/kiro:spec-requirements`

**目的**：根据功能描述生成 EARS 格式的完整、可测试的需求。

**参数**：`<功能名称>`

**用法**：
```bash
/kiro:spec-requirements <功能名称>
```

**参数说明**：
- `<功能名称>` (必填)：来自 `/kiro:spec-init` 的功能目录名称。

**EARS 格式** (Easy Approach to Requirements Syntax)：
```
WHEN <触发条件> THE <系统> SHALL <动作>
IF <前提条件> THEN THE <系统> SHALL <动作>
WHERE <特定场景> THE <系统> SHALL <动作>
THE <系统> SHALL <动作>
```

---

### `/kiro:spec-design`

**目的**：创建全面的技术设计，将需求 (WHAT) 转化为架构设计 (HOW)。

**参数**：`<功能名称> [-y]`

**用法**：
```bash
/kiro:spec-design <功能名称> [-y]
```

**参数说明**：
- `<功能名称>` (必填)：功能目录名称
- `[-y]` (可选)：自动批准需求而无需确认

---

### `/kiro:spec-tasks`

**目的**：生成详细、可操作的实现任务，将设计转化为可执行的工作项，包括标记为 `P0`、`P1` 等的并行友好波次。

**参数**：`<功能名称> [-y]`

**用法**：
```bash
/kiro:spec-tasks <功能名称> [-y]
```

---

### `/kiro:spec-impl`

**目的**：使用测试驱动开发 (TDD) 方法执行实现任务。

**参数**：`<功能名称> [任务编号]`

**用法**：
```bash
/kiro:spec-impl <功能名称> [任务编号]
```

**参数说明**：
- `<功能名称>` (必填)：功能目录名称
- `[任务编号]` (可选)：要执行的逗号分隔的任务 ID
  - 格式：`1.1,1.2,1.3` 或 `1,2,3`
  - 如果省略：执行所有待处理（未勾选）的任务

**TDD 循环**：
1. **🔴 RED** - 先写失败的测试
2. **🟢 GREEN** - 编写通过测试的最少代码
3. **🔵 REFACTOR** - 在保持测试通过的同时重构代码
4. **✅ VERIFY** - 确保没有回归
5. **📝 MARK COMPLETE** - 更新 `tasks.md` 中的复选框

---

## 验证命令 (Validation Commands)

### `/kiro:validate-gap`

**目的**：分析需求与现有代码库之间的差距，为实现策略提供参考（针对现有项目的可选质量关卡）。

### `/kiro:validate-design`

**目的**：交互式技术设计质量评审，确保已准备好进行实现（可选质量关卡）。

### `/kiro:validate-impl`

**目的**：根据需求、设计和任务验证实现情况，确保质量和完整性。

---

## 状态与工具 (Status & Utility)

### `/kiro:spec-status`

**目的**：显示规格在所有阶段的完整状态和进度。

---

## 工作流示例

### 完整绿地项目流 (Greenfield Flow)
1. `/kiro:steering`
2. `/kiro:spec-init <描述>`
3. `/kiro:spec-requirements <feature>`
4. `/kiro:spec-design <feature>`
5. `/kiro:spec-tasks <feature>`
6. `/kiro:spec-impl <feature> 1.1,1.2`
7. `/kiro:spec-status <feature>`
8. `/kiro:validate-impl <feature>`

### 棕地项目流 (现有项目)
1. `/kiro:steering`
2. `/kiro:steering-custom`
3. `/kiro:spec-init <描述>`
4. `/kiro:spec-requirements <feature>`
5. `/kiro:validate-gap <feature>`
6. `/kiro:spec-design <feature>`
7. `/kiro:validate-design <feature>`
8. `/kiro:spec-tasks <feature>`
9. `/kiro:spec-impl <feature>`

---

## 故障排除

### "Feature not found"
**原因**：功能名称错误或规格不存在
**解决方法**：检查 `.kiro/specs/` 目录获取正确名称

### "Requirements/Design not approved"
**原因**：在 `spec.json` 中未标记为已批准
**解决方法**：评审文档并批准，或使用 `-y` 标志

---

## 快速参考卡片

```
┌─────────────────────────────────────────────────────────────┐
│ cc-sdd 命令快速参考                                         │
├─────────────────────────────────────────────────────────────┤
│ 项目引导 (STEERING)                                         │
│  /kiro:steering              创建/更新项目记忆              │
│  /kiro:steering-custom       添加特定领域的上下文           │
├─────────────────────────────────────────────────────────────┤
│ 规格工作流 (SPEC WORKFLOW)                                  │
│  /kiro:spec-init <desc>      初始化功能                     │
│  /kiro:spec-requirements <f> 生成需求                       │
│  /kiro:spec-design <f> [-y]  创建技术设计                   │
│  /kiro:spec-tasks <f> [-y]   拆解为实现任务                 │
│  /kiro:spec-impl <f> [tasks] 通过 TDD 执行实现              │
├─────────────────────────────────────────────────────────────┤
│ 验证 (VALIDATION - 可选)                                    │
│  /kiro:validate-gap <f>      分析现有与需求的差距           │
│  /kiro:validate-design <f>   评审设计质量                   │
│  /kiro:validate-impl [f] [t] 验证实现质量                   │
├─────────────────────────────────────────────────────────────┤
│ 状态 (STATUS)                                               │
│  /kiro:spec-status <f>       检查进度                       │
└─────────────────────────────────────────────────────────────┘

图例: <f> = 功能名称, [t] = 任务编号, [-y] = 自动批准
```

---

**最后更新**: 2025-10-29  
**版本**: 2.0.0-alpha.3
