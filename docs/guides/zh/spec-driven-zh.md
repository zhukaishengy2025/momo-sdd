# 规格驱动开发工作流 (WIP)

> 📖 **English version here:** [Spec-Driven Development Workflow](spec-driven.md)

本文档说明了 cc-sdd 如何在 AI 驱动开发生命周期 (AI-DLC) 中实现规格驱动开发 (SDD)。在决定运行哪个斜杠命令、审查哪个产物以及如何让团队适应工作流时，请参考本文档。

## 生命周期概览 (Lifecycle Overview)

1. **引导 (Steering - 上下文捕获)** – `/kiro:steering` 和 `/kiro:steering-custom` 将架构、规范和领域知识收集到引导文档中。
2. **规格初始化 (Spec Initiation)** – `/kiro:spec-init <feature>` 创建功能工作区 (`.kiro/specs/<feature>/`)。
3. **需求 (Requirements)** – `/kiro:spec-requirements <feature>` 收集澄清信息并生成 `requirements.md`。
4. **设计 (Design)** – `/kiro:spec-design <feature>` 首先输出/更新 `research.md`（包含调查笔记，无需研究时跳过），然后生成 `design.md` 供人工审批。
5. **任务规划 (Task Planning)** – `/kiro:spec-tasks <feature>` 创建 `tasks.md`，将交付物映射到可执行的块，并使用 `P0`、`P1` 等标记每个波次，以便团队了解哪些任务可以并行运行。
6. **实现 (Implementation)** – `/kiro:spec-impl <feature> <task-ids>` 驱动执行和验证。
7. **质量门禁 (Quality Gates)** – 可选的 `/kiro:validate-gap` 和 `/kiro:validate-design` 命令在实现之前将需求/设计与现有代码进行比较。
8. **状态跟踪 (Status Tracking)** – `/kiro:spec-status <feature>` 总结进度和审批情况。

> 需要一键完成？`/kiro:spec-quick <feature>` 在 Subagent 支持下编排步骤 2-5，在每个阶段后暂停审批，以便您微调生成的文档。

每个阶段都会暂停以供人工审查，除非您明确绕过它（例如通过传递 `-y` 或 CLI 的 `--auto` 标志）。由于规格驱动开发依赖这些门禁进行质量控制，请在生产工作中保留手动审批，仅在严密控制的实验中使用自动审批。团队可以将他们的审查清单嵌入到模板文件中，以便每个门禁都能反映本地的质量标准。

## 命令 → 产物映射表 (Command → Artifact Map)

| 命令 | 用途 | 主要产物 |
|---------|---------|---------------------|
| `/kiro:steering` | 构建/刷新项目记忆 | `.kiro/steering/*.md`
| `/kiro:steering-custom` | 添加领域特定引导 | `.kiro/steering/custom/*.md`
| `/kiro:spec-init <feature>` | 开始一个新功能 | `.kiro/specs/<feature>/`
| `/kiro:spec-requirements <feature>` | 捕获需求和差异 | `requirements.md`
| `/kiro:spec-design <feature>` | 生成调查日志 + 实现设计 | `research.md` (需要时), `design.md`
| `/kiro:spec-tasks <feature>` | 将设计分解为带并行波次的任务 | `tasks.md` (带有 P 标签)
| `/kiro:spec-impl <feature> <task-ids>` | 实现特定任务 | 代码 + 任务更新
| `/kiro:validate-gap <feature>` | 与现有代码的可选差异分析 | `gap-report.md`
| `/kiro:validate-design <feature>` | 可选的设计验证 | `design-validation.md`
| `/kiro:spec-status <feature>` | 查看阶段、审批和未完成任务 | CLI 摘要

## 自定义工作流 (Customising the Workflow)

- **模板 (Templates)** – 调整 `{{KIRO_DIR}}/settings/templates/{requirements,design,tasks}.md` 以反映您的审查流程。cc-sdd 会将这些复制到每个 spec 中。
- **审批 (Approvals)** – 在模板页眉中嵌入清单或所需的签核。Agent 将在每个阶段中展示它们。
- **产物 (Artifacts)** – 使用额外部分（风险日志、测试计划等）扩展模板，使生成的文档符合公司标准。

## 新项目 vs 现有项目 (New vs Existing Projects)

- **绿地项目 (Greenfield)** – 如果您已经有项目范围的护栏，请先通过 `/kiro:steering` (以及 `/kiro:steering-custom`) 捕获它们；否则立即从 `/kiro:spec-init` 开始，并随着这些规则变得清晰而让引导文档自然演进。
- **棕地项目 (Brownfield)** – 从 `/kiro:validate-gap` 和 `/kiro:validate-design` 开始，以确保新规格在实现前与现有系统保持一致。

## 相关资源 (Related Resources)

- [README 中的快速开始](../../README_zh.md#-快速开始)
- [Claude Code Subagents 工作流](claude-subagents-zh.md)
